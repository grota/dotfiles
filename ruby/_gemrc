# To checkout gems' docs:
# `gem install yard`
# `yard server -g`
#
# Old info:
# use `gem rdoc gherkin --rdoc --ri` to create rdoc/ri docs that you
# can later view via `gem server`.
# Use `rvm docs generate-gems` to do it for all the gems in the current gemset.
# seems to be the same as `gem rdoc --all --rdoc --ri`
gem: --no-ri --no-rdoc
