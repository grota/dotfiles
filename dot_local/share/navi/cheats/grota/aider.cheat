% aider

# aider with model
aider --model <model>

# aider dff meld conf with upstream
meld ~/.aider.conf.yml https://raw.githubusercontent.com/Aider-AI/aider/refs/heads/main/aider/website/assets/sample.aider.conf.yml

; for some reason we need to skip the first 3 lines instead of 2 like it should be.
$ model: aider --list-models '/' | tail -n +3 | cut -c 3- --- --prevent-extra
