% docker

# Remove an image
docker image rm <image_id>

# docker cleanup
docker builder prune -f; docker system prune --volumes -f -a

$ image_id: docker images --- --headers 1 --column 3 --multi --expand
