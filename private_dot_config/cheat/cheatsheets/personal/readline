      +-------------+
      | WORD | word |
+-----+-------------+
|Left | C-w  | C-d  |
|-----+-------------+
|Right| M-w  | M-d  |
+-----+-------------+

M-hl to move left/right charwise like in vim

C-x C-s add parameters to the last command
C-x q quote params

M-# comment line

# bind -P/p: dump-functions  (bind -p|\grep -v '^#'|\grep -v 'self-insert')
